import { PropositionInitial } from './proposition-initial.model';

describe('PropositionInitial', () => {
  it('should create an instance', () => {
    expect(new PropositionInitial()).toBeTruthy();
  });
});
